-- construct.js



- Watching for Store Variable saved in mapping.storeVariable
- Turning value saved in mapping.mutationToFalse back to false
- In the raycaster file, values for "active" in blocksArray are set to true, if they are active
- If there are "active = true" properties do following ...
- With all objects, which have the property "active=true" check for the name. Take the object with this name in blocksModel and do the following
